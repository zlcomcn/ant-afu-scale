name: scale_app
description: 蓝牙体脂秤 App (逆向蚂蚁阿福协议)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.32.8
  permission_handler: ^11.3.0
  provider: ^6.1.1
  shared_preferences: ^2.2.3
  home_widget: ^0.7.0
  intl: ^0.19.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
